import type { AppContainer } from '@open-mercato/shared/lib/di/container'

// OpenCare DI registrar. Phase 1 has no module-specific services yet.
export function register(_container: AppContainer) {
  // Reserved for future OpenCare services (compliance integration, etc.).
}
